package model;

import model.Player.Player;

public class SkaterStat extends Stat{

    public SkaterStat(int weekNumber, Player player, String eventType) {
        super(weekNumber, player, eventType);
    }
}
